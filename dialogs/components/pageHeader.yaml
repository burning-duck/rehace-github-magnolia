# Automatically generated dialog with few most often used fileds (text, right, link, DAM image, multivalue, link to app, select). Feel free to modify them for your specific component.

form:
  label: Page Header
  tabs:
    - name: tabMain
      label: Main
      fields:

        - name: title
          class: info.magnolia.ui.form.field.definition.TextFieldDefinition
          label: Title

        - name: description
          class: info.magnolia.ui.form.field.definition.TextFieldDefinition
          label: Description

        - name: overwrite
          class: info.magnolia.ui.form.field.definition.CheckboxFieldDefinition
          label: Overwrite
          buttonLabel: Overwrite with page project data?
          description: If checked, title and description are taken from current project data.

        - name: inverted
          class: info.magnolia.ui.form.field.definition.CheckboxFieldDefinition
          label: Inverted
          buttonLabel: Use inverted UI?

        - name: icon
          class: info.magnolia.ui.form.field.definition.TextFieldDefinition
          label: Icon
          description: Use an icon from semantic-ui.

        - name: textAlign
          class: info.magnolia.ui.form.field.definition.SelectFieldDefinition
          label: Text Align
          options:
            - name: Left
              value: left
            - name: Center
              value: center
            - name: Right
              value: right
            - name: Justified
              value: justified


actions:
  commit:
    class: info.magnolia.ui.admincentral.dialog.action.SaveDialogActionDefinition
  cancel:
    class: info.magnolia.ui.admincentral.dialog.action.CancelDialogActionDefinition
